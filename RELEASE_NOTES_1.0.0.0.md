# HappyTrigger 1.0.0.0 正式版

## 変更内容
- インストーラーの説明・短い紹介文・タグを日本語化。
- 攻撃1～8、足止め1～3、禁止1～2、四角・丸・プラス・三角の17種を、ゲーム内アイコンと日本語名で選択。
- 「ナイト＜PLD＞」などのジョブを指定して、自分を含むパーティー内の該当メンバーへマーカーを付与。
- 条件合致時に任意の本文をSay・パーティー・アライアンス・自分のみ（エコー）へ送信。
- ネイビーを基調としたダークテーマ、青緑のアクセント、角丸の入力欄・ボタン、余白・タブ・ヘッダーを統一。
- タイトルバーに「HappyTrigger v1.0.0.0 | 正式版」を表示。登録数・有効数・ボックス数をヘッダーに表示。
- 設定保存、複製、CSV入出力、詳細一覧に新項目を反映。旧設定では新機能は無効。
- 旧CSVの任意列省略を誤って拒否していたヘッダー数チェックを修正。

## 設定例
1. `/happytrigger` で設定画面を開き、「ログトリガー」→「01 検知条件」で検知する言葉に「テスト」を入力。
2. 「02 実行内容」の「合致時の追加アクション」で「対象ジョブへターゲットマーカーを付与」を有効化。
3. マーカーを「攻撃1」、対象ジョブを「ナイト＜PLD＞」に設定。
4. 「チャットを自動送信」を有効化し、送信先と本文「テスト」を設定して保存。
5. 該当するチャットが届くと追加動作が実行されます。表示用テキスト・画像を空欄にすると追加動作のみでも使えます。

## 動作の詳細
- 追加動作は条件成立後のFramework更新時に実行します。表示待機時間とは独立です。
- 同一ジョブが複数いる、不在、対象が読み込まれていない場合はマーカーをスキップして内部ログへ記録します。ソロでは自分を対象にします。
- 同じマーカーが既に対象へ付いている場合は再実行しません。パーティーの表示順はゲーム側の代名詞解決で判定します。
- 自動送信した本文に一致する自己送信ログは30秒間、トリガー判定から除外します。この間は手動で送った同一本文も対象になります。
- 本文はUTF-8で450バイトまで（一般的な日本語で約150文字）。改行・制御文字・先頭のスラッシュ・山括弧は使用できません。コマンドや代名詞の展開には対応していません。
- 追加動作は全体で1秒ごとに1件処理し、待機上限32件、10秒を過ぎた要求は破棄します。ログアウトやエリア変更後に古い動作を実行しません。
- 「テスト」「位置設定」は実際のマーカー付与・チャット送信をしません。
- ゲーム側の送信・マーカー制限やパーティー参加状態により、要求が受理されない場合があります。

## 配布・公開
- `HappyTrigger.zip`：DalamudPackagerで生成したインストール用ZIP。
- `HappyTrigger-1.0.0.0-source.zip`：更新済みソース。古いbin/objは除外しています。
- `repo.json`：インストーラー用の更新済み配布情報。
- GitHubへのアップロード・リリース公開は実施していません。
- 公開時はタグ `v1.0.0.0` のリリースに `HappyTrigger.zip` を添付した後、リポジトリの `repo.json` を更新してください。先にrepo.jsonを公開するとダウンロード先が未作成になります。

## 検証結果
- .NET SDK 10.0.300 / ローカルDalamud API 15でReleaseビルド成功（警告0・エラー0）。
- 21項目の自動検証成功：旧JSONの既定値、新項目の複製・JSON往復、CSV列整合、引用符を含むCSV往復、旧CSV読み込み、表示なし動作、本文検証、UTF-8境界、無効マーカー、バージョン、自己出力判定と期限切れ・再入抑止。
- ゲームを起動した状態でのUI表示・実際の送信・マーカー付与は未検証です。

## ゲーム内確認手順
1. 旧設定を保持したまま読み込み、タイトルバー・テーマ・日本語インストーラー情報を確認。
2. マーカー一覧の17アイコンとジョブ一覧、設定の保存・再起動後の維持を確認。
3. 送信先を「自分のみ（エコー）」、トリガーと本文を「テスト」に設定。手動で `/echo テスト` を一度実行し、自動エコーが一度だけ出ることを確認。
4. 自分のジョブを対象にマーカーを設定し、同じ条件で付与を確認。再発火で既存マーカーが消えないことも確認。
5. パーティー内で対象ジョブが1名・0名・複数名の場合、およびパーティー表示順変更後の付与先を確認。
6. チャットログ条件とFFXIV Log条件の両方、CSVの書き出し・取り込み、表示テストで外部動作が発生しないことを確認。

## 実装時の参照資料
- マーカーのコマンド仕様：https://na.finalfantasyxiv.com/lodestone/playguide/db/text_command/4348eed9de5/
- クライアント構造体：https://github.com/aers/FFXIVClientStructs

## ログトリガー画面のリデザイン（バージョン据え置き）
バージョンは1.0.0.0のままです。

入力画面を次の4分類に分けました。
1. **01 検知条件**：反応させたいログ・言葉、一致方法。場所・前提トリガーは詳細欄に収納。
2. **02 実行内容**：表示する画像・文章、読み上げ、残り時間、マーカー、チャット本文。
3. **03 見た目・位置**：表示秒数・待機秒数、座標、文字デザイン・画像サイズ。座標やフォント品質は折りたたみ。
4. **04 名前・整理**：管理名、有効状態、所属ボックス・ラベル。

分類タブと保存・表示テストはスクロール領域の外に置き、各分類を個別にスクロールできます。入力が不足している場合は分類名付きの案内を表示し、保存ボタンを無効にします。入力例を追加し、ログ検知用の言葉と出力用の文章を区別しました。元サイズで画像を表示する場合は、不要な幅・高さの入力欄を隠します。

ビルド成功（警告0・エラー0）、既存の21項目の検証が通過しています。ゲーム内での画面描画は未確認です。
同じ1.0.0.0から差し替える場合、インストーラーの通常のバージョン比較では更新候補にならないため、再インストールまたはプラグイン停止後のファイル差し替えが必要です。
